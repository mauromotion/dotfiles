#!/bin/bash

cd "$HOME/.zsh/plugins/zsh-vi-mode/" && git pull
cd "$HOME/.zsh/plugins/zsh-completions/" && git pull
cd "$HOME/.zsh/plugins/zsh-autosuggestions/" && git pull
cd "$HOME/.zsh/plugins/zsh-syntax-highlighting/" && git pull
cd "$HOME/.zsh/plugins/fzf-zsh-plugin/" && git pull
cd "$HOME/.zsh/themes/pure/" && git pull
